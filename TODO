 - Make the ReceiveMessage query allow a choice of attributes.
 - Write functional tests against a dummy system that responds with canned answers.
 - Write channel wrappers.
 - Write a short program package called "sqspipe" that takes two queue names as
   parameters and does nothing but transit data between stdin and stdout.
